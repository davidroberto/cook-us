{
  "name": "cook-us",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "install:all": "npm i --prefix backend && npm i --prefix frontend && npm i --prefix mobile && npm i --prefix e2e",
    "docker:run": "docker compose --env-file .env.local -f docker-compose.dev.yml up"
  },
  "devDependencies": {
    "husky": "^9.1.0"
  }
}
